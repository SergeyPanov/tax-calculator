services:
  tax-calculator:
    image: xpanov00/tax-calculator:latest
    expose:
      - 8000
    deploy:
      replicas: 3
    restart: unless-stopped
    environment:
      ANNUAL_THRESHOLD_CZK: 1676052
      APP_ENV: production
      CORS_ALLOWED_ORIGINS: "*"
